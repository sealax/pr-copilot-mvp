# PR Copilot MVP

Minimal Next.js MVP for an AI-assisted PR drafting tool.

## What works right now
- Local Next.js app runs on http://localhost:3000
- API endpoint: `POST /api/generate`
- Generates a press release-style draft from a brief using OpenAI

## How to run locally
1) Install dependencies
```bash
npm install

